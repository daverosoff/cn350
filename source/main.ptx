<?xml version='1.0' encoding='utf-8'?>

<pretext xml:lang="en-US" xmlns:xi="http://www.w3.org/2001/XInclude">

  <docinfo>
    <macros>
      \newcommand{\R}{\mathbb R}
      \newcommand{\N}{\mathbb N}
      \newcommand{\abs}[1]{\lvert #1 \rvert}
      %\newcommand{\and}[2]{(#1) \, \mathtt{and} \, (#2)}
      %\newcommand{\or}[2]{(#1) \, \mathtt{or} \, (#2)}
      %\newcommand{\not}[1]{\mathtt{not} \, (#1)}
      \newcommand{\aster}[1]{#1^{\ast}}
      \newcommand{\eps}{\varepsilon}
      \newcommand{\lsim}{\mathord{\sim}}
    </macros>
    <latex-image-preamble>
      \usepackage{tikz}
      \usetikzlibrary{automata,positioning,arrows}
      \usetikzlibrary{patterns}
      \tikzset{node distance=2.0cm, % Minimum distance between two nodes. Change if necessary.
        every state/.style={ % Sets the properties for each state
        semithick,
        fill=gray!20},
        initial text={}, % No label on start arrow
        double distance=2pt, % Adjust appearance of accept states
        every edge/.style={ % Sets the properties for each transition
        draw,
        ->,>=stealth', % Makes edges directed with bold arrowheads
        auto,
        semithick}}
        \let\epsilon\varepsilon
      \usepackage{amsmath}
    </latex-image-preamble>
  </docinfo>

  <book xml:id="dwr-cn-350">
    <title>Foundations of computation</title>
    <subtitle>DRAFT EDITION</subtitle>

    <xi:include href="meta/frontmatter.ptx" />
    <xi:include href="00-Formal-Proof/main.ptx" />
    <xi:include href="01-DFA/main.ptx" />
    <xi:include href="99-Assignments/main.ptx" />
  </book>
</pretext>
